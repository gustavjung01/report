# Source structure

This branch adds a clean source folder layout.

- core: shared app code
- modules: business screens
- styles: CSS files
- config: public frontend config

Root should stay limited to public deploy files only.
