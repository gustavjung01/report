// Disabled emergency: this forced layer was making the UI larger and interfering with layout.
// Keep the file as a harmless shim because service worker/import lists may still reference it.
export {};
